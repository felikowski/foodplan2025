import { RecipeIngredientEntity } from './recipe-ingredient.entity';

describe('RecipeIngredientEntity', () => {
  it('should be defined', () => {
    expect(new RecipeIngredientEntity()).toBeDefined();
  });
});
